<<<<<<< HEAD
# SDGW_CP-ABE-SSE

### (CP-ABE)
```shell
./run.sh
```


### (SSE) 

1. Start the Server

```shell
cd Server && make
./server [par] # Use one terminal, where par can be invIndex_data.txt, invIndex_data1.txt..
```

2. Start the Client

```shell
cd Client && make
./client [keyword] # Open a new terminal, where keyword can be any word in the text file
```
=======
# SDGWP-CP-ABE-SS
~                        
